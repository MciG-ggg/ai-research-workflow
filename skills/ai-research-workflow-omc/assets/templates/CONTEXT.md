# Project Context

> Ubiquitous language for this research portfolio. Borrowed from DDD: every
> term defined here is the project's single source of truth for vocabulary.
> Use these terms exactly when speaking to the agent, in artifacts, and in
> commits. When you introduce a new term, add it here first.

## Project Snapshot

- Project name: TODO
- Research domain: TODO
- Primary outputs (paper / internal report / benchmark / blog): TODO
- Current north-star question (1 sentence): TODO

## Ubiquitous Language

> For each term: the canonical name, a one-line definition, and terms to
> explicitly avoid because they cause confusion. Keep this list short
> (≤ 30 rows). If a term needs a paragraph, link out instead.

| Canonical term | Definition | Avoid (legacy / overloaded) |
| --- | --- | --- |
| TODO | TODO | TODO |

## Active Research Questions

> The 1-5 questions that define what we are currently trying to answer.
> Each row should link to the workstream or `RESEARCH.md` section that
> owns it. When a question is retired, move it to "Retired Questions"
> at the bottom — do not delete.

| ID | Question | Owner workstream | Status |
| --- | --- | --- | --- |
| Q1 | TODO | TODO | TODO |

## Acronyms and Project-local Jargon

> Acronyms whose expansion is NOT obvious from context. One row per
> acronym. Skip universally-known terms (GPU, CPU, JSON, YAML).

| Acronym | Expansion | Notes |
| --- | --- | --- |
| TODO | TODO | TODO |

## Conventions and Defaults

> Project-level defaults that every agent and human should follow
> without asking. Examples: seed default, dataset path convention,
> review checklist, naming convention for runs.

- TODO

## Sources of Truth (where to look things up)

> Pointers so the agent never has to ask "where do I write X?".
> Keep this short — every entry should resolve the question in one hop.

| Question | Look here |
| --- | --- |
| What is this project about? | `RESEARCH.md` (portfolio) |
| Which workstreams are active? | `INDEX.md` (portfolio) |
| Why are we running experiment X? | `.ai-research-workflow/<slug>/EXPERIMENT.md` |
| What did we conclude last time? | `.ai-research-workflow/<slug>/RESULTS.md` |
| What is this run directory for? | `.ai-research-workflow/<slug>/runs/<id>/purpose.md` |
| What decisions did we make? | `DECISIONS.md` (portfolio + per-workstream) |
| What does term Y mean? | This file (Ubiquitous Language table) |

## Last Updated

TODO